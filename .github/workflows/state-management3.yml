name: State Management 3

on:
  pull_request:
    types: [unlabeled]

jobs:
  open-tag:
    runs-on: ubuntu-latest
    if: |
      (github.event.pull_request.user.login == 'BoweiZhang-at')
    steps:
      - name: Full Context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
